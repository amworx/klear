<?xml version="1.0" encoding="utf-8"?>
<!-- Klear launch screen: brand-cyan only. The animated splash mark is the
     Lottie file shown by KlearSplashPage (assets/lottie/splash.json), so we
     keep this native layer free of any logo to avoid flashing the old
     branding before Flutter paints. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/klear_launch_bg" />
</layer-list>