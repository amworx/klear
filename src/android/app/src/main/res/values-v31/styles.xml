<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash: brand cyan background + water drop icon. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/klear_launch_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/klear_launch_logo</item>
    </style>
</resources>